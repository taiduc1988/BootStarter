<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>1.3.5.RELEASE</version>
        <relativePath></relativePath>
    </parent>
    <groupId>com.broadleafcommerce</groupId>
    <artifactId>broadleaf-basic-boot-starter-parent</artifactId>
    <version>5.2.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>BroadleafCommerce Spring Boot Starter Parent</name>
    <description>Contains configuration for a Broadleaf Commerce Spring Boot starter project</description>

    <properties>
        <hibernate.version>4.1.11.Final</hibernate.version>
        <solr.version>5.3.1</solr.version>
        <hsqldb.version>2.3.2</hsqldb.version>
        <solr.starter.version>5.2.0-SNAPSHOT</solr.starter.version>
        <admin.starter.version>5.2.0-SNAPSHOT</admin.starter.version>
        <reference.starter.version>5.2.0-SNAPSHOT</reference.starter.version>
        <database.starter.version>5.2.0-SNAPSHOT</database.starter.version>
        <broadleaf.bom.version>5.2.0-SNAPSHOT</broadleaf.bom.version>
    </properties>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.zeroturnaround</groupId>
                    <artifactId>jrebel-maven-plugin</artifactId>
                    <version>1.1.3</version>
                    <dependencies>
                        <dependency>
                            <groupId>org.codehaus.plexus</groupId>
                            <artifactId>plexus-utils</artifactId>
                            <version>1.4.1</version>
                            <scope>runtime</scope>
                        </dependency>
                    </dependencies>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.broadleafcommerce</groupId>
                <artifactId>broadleafcommerce-boot-starter-admin</artifactId>
                <version>${admin.starter.version}</version>
            </dependency>
            <dependency>
                <groupId>com.broadleafcommerce</groupId>
                <artifactId>broadleafcommerce-boot-starter-solr</artifactId>
                <version>${solr.starter.version}</version>
            </dependency>
            <dependency>
                <groupId>com.broadleafcommerce</groupId>
                <artifactId>broadleafcommerce-boot-starter-database</artifactId>
                <version>${database.starter.version}</version>
            </dependency>
            <dependency>
                <groupId>com.broadleafcommerce</groupId>
                <artifactId>reference-boot-starter-admin</artifactId>
                <version>${reference.starter.version}</version>
            </dependency>
            <dependency>
                <groupId>com.broadleafcommerce</groupId>
                <artifactId>reference-boot-starter-site</artifactId>
                <version>${reference.starter.version}</version>
            </dependency>
            <dependency>
                <groupId>com.broadleafcommerce</groupId>
                <artifactId>broadleaf-enterprise-bom</artifactId>
                <version>${broadleaf.bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <modules>
        <module>broadleaf-commercial-boot-starter-parent</module>
    </modules>
</project>
